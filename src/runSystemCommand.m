%{
Purpose: This function runs system commands and checks to make sure that
there are no attempts to do anything that shouldn't be done
%}

function [successFlag, output] = runSystemCommand(command,quietFlag)

    

end